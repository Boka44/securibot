

module.exports = (bot, commands, db) => {
    // commands.global.push({ command: "start", description: "Start the bot" });
    // bot.command("start", (ctx) => {
    //   ctx.reply("Welcome! Use /setup to configure the portal.");
    // });
  };